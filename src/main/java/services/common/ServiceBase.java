package services.common;

/**
 * Marker interface for service-layer classes.
 *
 * Some services implement this to share a common type without enforcing a CRUD contract.
 */
public interface ServiceBase {
}
